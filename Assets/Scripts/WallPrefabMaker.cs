using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WallPrefabMaker : MonoBehaviour
{
    /*
    // This scripts allows you to change the length and width of the walls prefabs.
    [SerializeField]
    GameObject Wall;
    RayChecker raychecker;
    void Start()
    {
        raychecker = GetComponent<RayChecker>();
        Wall.transform.localScale = new Vector3 (0.01f, 1, raychecker.pathwid);
    }

    // Update is called once per frame
    */
}
